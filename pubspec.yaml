name: diKapo
description: Eine App, um die Artikel im diKapo Shop zu verwalten.
version: 0.3+3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
      sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
#  cupertino_icons: ^0.1.0
  http: "^0.11.3+16"
  image_picker:
  connectivity:
#  fluro: "^1.3.1"
  image:
  shared_preferences: "0.4.1"
 # zoomable_image: "1.2.0"
  crypto: any
  flutter_native_image:
    git: https://github.com/btastic/flutter_native_image.git
  image_cropper: ^0.0.9
#  fluttertoast: 2.1.4
  vibrate: ^0.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter



flutter:
  assets:
    - "assets/Logo - 11.01.18.png"
    - "assets/1x1.png"

  uses-material-design: true

